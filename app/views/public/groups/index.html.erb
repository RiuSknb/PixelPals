<div class='container'>
  <h1>グループ一覧</h1>

  <%= link_to "新しいグループを作成", new_group_path %>

  <ul>
    <% @groups.each do |group| %>
      <li>
        <%= link_to group.name, group_path(group) %>
        <%= group.body %>
      </li>
    <% end %>
  </ul>
</div>